# Copy to backend/.env.postgres (gitignored) — Postgres URI only, for: npm run db:deploy:remote
# Supabase: Dashboard → Project Settings → Database → Connection string (URI). Often add sslmode=require.
DATABASE_URL="postgresql://postgres.USER:PASSWORD@HOST:5432/postgres"
