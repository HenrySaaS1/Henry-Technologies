# Backend base URL (no trailing slash). Set in Azure Static Web Apps / CI as build-time env.
VITE_API_URL=http://localhost:5000

# Optional: Supabase browser client (`src/utils/supabase/client.js`, `@supabase/ssr` + `@supabase/supabase-js`).
# Use `frontend/.env.local` (gitignored) for real values — do not commit keys.
# VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
# VITE_SUPABASE_ANON_KEY=your_anon_or_publishable_key

# Backend Postgres (Prisma) remains in `backend/.env` as `DATABASE_URL`; that is separate from these client keys.

# Subdirectory hosting (e.g. https://henrysaas.com/hms1/). Must start with / and usually end with /
# VITE_BASE_PATH=/hms1/

# Set to false to test real sign-in during npm run dev (default dev = open workspace without login).
# VITE_BYPASS_AUTH=false

# Optional default tenant when the hostname does not map a workspace (e.g. azurestaticapps.net previews).
# Ignored on localhost. For goaskhenry.com + harland.* on the same SWA build: leave unset — host wins (see apiClient getTenantSlug).
# VITE_TENANT_SLUG=harland

# Dev: same-origin /api is proxied to the backend. Use only if the API is not on 127.0.0.1:5000.
# VITE_DEV_API_PROXY_TARGET=http://127.0.0.1:5000
# Set to `true` to call VITE_API_URL (or :5000) from the browser instead of the proxy (CORS must allow the dev origin + port).
# VITE_API_DIRECT=true

# Production demo: Sign In / Sign Up show Dashboard #2 · #3 · #10 · Harland previews (no DB). On by default in prod; set false to hide.
# VITE_DASHBOARD_DEMOS=false

# Odoo HENRY Events screen (opens from the Alerts tab).
# VITE_ODOO_EVENTS_URL=https://henrytechnologies.odoo.com/odoo/action-361

# Optional client-side preview gate (password is embedded in the built JS — not real security).
# Also enable the wrapper in `src/main.jsx`: `ENABLE_SITE_GATE = true`.
# Leave unset or empty to disable. For Azure SWA, set GitHub secret VITE_SITE_GATE_PASSWORD and pass it in the workflow env.
# VITE_SITE_GATE_PASSWORD=
