# goaskhenry.com — small site changes

Tracking minor fixes and polish for the live site (outside of large feature work).

## List

- **Site icon (favicon / browser tab icon)** — The current icon on the site uses a **white background**. Switch to a **transparent** background so it looks correct on dark browser chrome and pinned tabs. Use the same transparent mark as elsewhere (e.g. header logo asset) and redeploy the Static Web App build so `/henry-logo.png` (or the chosen favicon path) is updated.
